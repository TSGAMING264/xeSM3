; ============================================================
; xeSM3 v0.1.0 - Public Release Configuration
; Created by TSGAMING264
; ============================================================
;
; Public toggle values:
;   0   = Disabled
;   100 = Enabled
;
; Keep PostProcessFix=100, then enable ONLY ONE Xbox profile at a time.
; Debug Xbox and Retail Xbox are alternatives; switch them one at a time.
;
; Release default: Debug Xbox PostFX restoration.
; ============================================================

[PostProcessing]
PostProcessFix=100
PostProcessRetailXbox=0
PostProcessDebugXbox=100
